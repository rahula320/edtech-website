{
  "name": "acmyx-website-deployment",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd ../client && npm install && npm run build && cp -r build/* ../vercel-deploy/public/"
  }
} 