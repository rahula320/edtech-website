{
  "framework": "create-react-app",
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "build",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/[^.]+", "dest": "/index.html" }
  ]
} 