{
  "framework": "create-react-app",
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "build",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/[^.]+", "dest": "/index.html" }
  ],
  "env": {
    "REACT_APP_API_URL": "https://acmyx-api.vercel.app"
  }
} 